# Provider Adapters

This file is retained only as a historical note.

The active runtime no longer uses the old app-side `reply_provider` adapter path. Step 2 replaced it with the Dockerized `model_gateway` service and named model routes.

See instead:

- [model_gateway.md](model_gateway.md)

[project]
name = "memllm-domain"
version = "0.1.0"
description = "Shared domain models for memLLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.11.1",
]

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
